# REST (Simple - uses variable names, no raw values shown)
#
# วิธีใช้:
#   Authorization ใช้ token จาก .env (ADMIN_TOKEN / USER_TOKEN) ไม่ต้องยิง Login ก่อน
#   แต่ token หมดอายุตามเวลา (~30 วัน) ถ้าเจอ 401/403 ให้ยิง 1.1 / 2.2 Login เอา token ใหม่
#   แล้วอัปเดตค่า ADMIN_TOKEN / USER_TOKEN ใน .env

@baseUrl = {{$dotenv BASE_URL}}

@adminEmail = {{$dotenv ADMIN_EMAIL}}
@adminPassword = {{$dotenv ADMIN_PASSWORD}}
@adminNewPassword = {{$dotenv ADMIN_NEW_PASSWORD}}

@userName = {{$dotenv USER_NAME}}
@userEmail = {{$dotenv USER_EMAIL}}
@userPassword = {{$dotenv USER_PASSWORD}}
@userNewPassword = {{$dotenv USER_NEW_PASSWORD}}


## 1. Admin Section

### 1.1 Login (Admin)
# @name adminLogin
POST {{baseUrl}}/admin/login
Content-Type: application/json

{
    "email": "{{adminEmail}}",
    "password": "{{adminPassword}}"
}

###
@adminToken = {{$dotenv ADMIN_TOKEN}}

### 1.2 Forgot Password (Admin)
POST {{baseUrl}}/admin/forgot-password
Content-Type: application/json

{
    "email": "{{adminEmail}}"
}

###
@adminResetToken = {{$dotenv ADMIN_RESET_TOKEN}}

### 1.3 Reset Password (Admin)
POST {{baseUrl}}/admin/reset-password
Content-Type: application/json

{
    "resetPasswordToken": "{{adminResetToken}}",
    "password": "{{adminNewPassword}}"
}

### 1.4 Profile (Admin)
GET {{baseUrl}}/admin/users/me
Authorization: Bearer {{adminToken}}

## 2. User Section

### 2.1 Signup (User)
POST {{baseUrl}}/api/auth/local/register
Content-Type: application/json

{
    "username": "{{userName}}",
    "email": "{{userEmail}}",
    "password": "{{userPassword}}"
}

### 2.2 Login (User)
# @name userLogin
POST {{baseUrl}}/api/auth/local
Content-Type: application/json

{
    "identifier": "{{userEmail}}",
    "password": "{{userPassword}}"
}

###
@userToken = {{$dotenv USER_TOKEN}}

### 2.3 Forgot Password (User)
POST {{baseUrl}}/api/auth/forgot-password
Content-Type: application/json

{
    "email": "{{userEmail}}"
}

###
@userResetToken = {{$dotenv USER_RESET_TOKEN}}

### 2.4 Reset Password (User)
POST {{baseUrl}}/api/auth/reset-password
Content-Type: application/json

{
    "code": "{{userResetToken}}",
    "password": "{{userNewPassword}}",
    "passwordConfirmation": "{{userNewPassword}}"
}

### 2.5 Profile (User)
GET {{baseUrl}}/api/users/me
Authorization: Bearer {{userToken}}

### 2.6 Change Password (User)
POST {{baseUrl}}/api/auth/change-password
Content-Type: application/json
Authorization: Bearer {{userToken}}

{
    "currentPassword": "{{userPassword}}",
    "password": "{{userNewPassword}}",
    "passwordConfirmation": "{{userNewPassword}}"
}

## 3. Content Section
# ทุก request ในส่วนนี้ใช้ @userToken จาก .env (USER_TOKEN) — ยิงได้ทันทีโดยไม่ต้อง login ก่อน
# หมายเหตุ (3.1.1 Create Student): cardId เป็น unique — ถ้ายิงแล้วเจอ 400 แปลว่า cardId ซ้ำกับ record เก่า
# ให้แก้ค่า STUDENT_CARD_ID ใน .env เป็นเลขใหม่ก่อนรัน (หรือลบ student เก่าผ่าน pgAdmin)

@studentName = {{$dotenv STUDENT_FIRSTNAME}} {{$dotenv STUDENT_LASTNAME}}
@studentCardId = {{$dotenv STUDENT_CARD_ID}}
@studentMobile = {{$dotenv STUDENT_MOBILE}}

@subjectName = {{$dotenv SUBJECT_NAME}}
@subjectNewName = {{$dotenv SUBJECT_NEW_NAME}}

@teacherName = {{$dotenv TEACHER_FIRSTNAME}} {{$dotenv TEACHER_LASTNAME}}

### 3.1 Student

### 3.1.1 Create (Student)
# @name createStudent
POST {{baseUrl}}/api/students
Content-Type: application/json
Authorization: Bearer {{userToken}}

{
    "data": {
        "name": "{{studentName}}",
        "mobile": "{{studentMobile}}",
        "cardId": "{{studentCardId}}"
    }
}

###
@studentId = {{createStudent.response.body.data.id}}

### 3.1.2 List All (Student)
GET {{baseUrl}}/api/students
Authorization: Bearer {{userToken}}

### 3.1.3 List with ID (Student)
GET {{baseUrl}}/api/students/{{studentId}}
Authorization: Bearer {{userToken}}

### 3.1.4 Update (Student)
PUT {{baseUrl}}/api/students/{{studentId}}
Content-Type: application/json
Authorization: Bearer {{userToken}}

{
    "data": {
        "name": "{{studentName}}"
    }
}

### 3.2 Subject

### 3.2.1 Create (Subject)
# @name createSubject
POST {{baseUrl}}/api/subjects
Content-Type: application/json
Authorization: Bearer {{userToken}}

{
    "data": {
        "name": "{{subjectName}}"
    }
}

###
@subjectId = {{createSubject.response.body.data.id}}

### 3.2.2 List All (Subject)
GET {{baseUrl}}/api/subjects
Authorization: Bearer {{userToken}}

### 3.2.3 List with ID (Subject)
GET {{baseUrl}}/api/subjects/{{subjectId}}
Authorization: Bearer {{userToken}}

### 3.2.4 Update (Subject)
PUT {{baseUrl}}/api/subjects/{{subjectId}}
Content-Type: application/json
Authorization: Bearer {{userToken}}

{
    "data": {
        "name": "{{subjectNewName}}"
    }
}

### 3.3 Teacher

### 3.3.1 Create (Teacher)
# @name createTeacher
POST {{baseUrl}}/api/teachers
Content-Type: application/json
Authorization: Bearer {{userToken}}

{
    "data": {
        "name": "{{teacherName}}"
    }
}

###
@teacherId = {{createTeacher.response.body.data.id}}

### 3.3.2 List All (Teacher)
GET {{baseUrl}}/api/teachers
Authorization: Bearer {{userToken}}

### 3.3.3 List with ID (Teacher)
GET {{baseUrl}}/api/teachers/{{teacherId}}
Authorization: Bearer {{userToken}}

### 3.3.4 Update (Teacher)
PUT {{baseUrl}}/api/teachers/{{teacherId}}
Content-Type: application/json
Authorization: Bearer {{userToken}}

{
    "data": {
        "name": "{{teacherName}}"
    }
}